import { getAccountIdFromContext } from '../../core/tenant-context.js';
import { createProdutoImagem, deleteProdutoImagem, listProdutoImagens, updateProdutoImagem } from './produto-imagens.repository.js';
import { recordAuditLog } from '../../core/audit-logs.js';

export async function getProdutoImagens(context = {}) { const accountId = getAccountIdFromContext(context); return { ok: true, ...await listProdutoImagens(context.params?.produtoId, { accountId }) }; }
export async function postProdutoImagem(context = {}) { const accountId = getAccountIdFromContext(context); const body = { ...(context.body || {}) }; delete body.account_id; delete body.accountId; delete body.tenant_id; delete body.owner_user_id; try { const item = await createProdutoImagem(context.params?.produtoId, body, { accountId }); await recordAuditLog(context, { modulo: 'produtos', entidade: 'produto', entidade_id: context.params?.produtoId || null, acao: 'upload_imagem', descricao: 'Imagem de produto enviada', status: 'success', sucesso: true, metadata: { imagem_id: item?.id || null } }).catch(() => null); return { ok: true, item }; } catch (error) { await recordAuditLog(context, { modulo: 'produtos', entidade: 'produto', entidade_id: context.params?.produtoId || null, acao: 'upload_imagem', descricao: 'Falha ao enviar imagem de produto', status: 'failed', sucesso: false, erro_codigo: error?.code || 'INTERNAL_SERVER_ERROR', erro_mensagem: error?.message || 'Erro ao enviar imagem' }).catch(() => null); throw error; } }
export async function patchProdutoImagem(context = {}) { const accountId = getAccountIdFromContext(context); const body = { ...(context.body || {}) }; delete body.account_id; delete body.accountId; delete body.tenant_id; delete body.owner_user_id; try { const item = await updateProdutoImagem(context.params?.produtoId, context.params?.imagemId, body, { accountId }); await recordAuditLog(context, { modulo: 'produtos', entidade: 'produto', entidade_id: context.params?.produtoId || null, acao: 'editar_imagem', descricao: 'Imagem de produto editada', status: 'success', sucesso: true, metadata: { imagem_id: context.params?.imagemId || null } }).catch(() => null); return { ok: true, item }; } catch (error) { await recordAuditLog(context, { modulo: 'produtos', entidade: 'produto', entidade_id: context.params?.produtoId || null, acao: 'editar_imagem', descricao: 'Falha ao editar imagem de produto', status: 'failed', sucesso: false, erro_codigo: error?.code || 'INTERNAL_SERVER_ERROR', erro_mensagem: error?.message || 'Erro ao editar imagem' }).catch(() => null); throw error; } }
export async function deleteProdutoImagemHandler(context = {}) { const accountId = getAccountIdFromContext(context); try { const result = await deleteProdutoImagem(context.params?.produtoId, context.params?.imagemId, { accountId }); await recordAuditLog(context, { modulo: 'produtos', entidade: 'produto', entidade_id: context.params?.produtoId || null, acao: 'remover_imagem', descricao: 'Imagem de produto removida', status: 'success', sucesso: true, metadata: { imagem_id: context.params?.imagemId || null } }).catch(() => null); return { ok: true, ...result }; } catch (error) { await recordAuditLog(context, { modulo: 'produtos', entidade: 'produto', entidade_id: context.params?.produtoId || null, acao: 'remover_imagem', descricao: 'Falha ao remover imagem de produto', status: 'failed', sucesso: false, erro_codigo: error?.code || 'INTERNAL_SERVER_ERROR', erro_mensagem: error?.message || 'Erro ao remover imagem' }).catch(() => null); throw error; } }

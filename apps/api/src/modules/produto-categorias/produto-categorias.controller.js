import { getAccountIdFromContext } from '../../core/tenant-context.js';
import { createProdutoCategoria, getProdutoCategoriaById, listProdutoCategorias, updateProdutoCategoria } from './produto-categorias.repository.js';
import { recordAuditLog } from '../../core/audit-logs.js';

export async function getProdutoCategorias(context = {}) { const accountId = getAccountIdFromContext(context); const result = await listProdutoCategorias({ status: context.query?.status }, { accountId }); return { ok: true, items: result.items, total: result.total }; }
export async function getProdutoCategoria(context = {}) { const accountId = getAccountIdFromContext(context); return { ok: true, item: await getProdutoCategoriaById(context.params?.id, { accountId }) }; }
export async function createProdutoCategoriaHandler(context = {}) { const accountId = getAccountIdFromContext(context); const body = { ...(context.body || {}) }; delete body.account_id; delete body.accountId; delete body.tenant_id; delete body.tenantId; delete body.owner_user_id; delete body.ownerUserId; try { const item = await createProdutoCategoria(body, { accountId }); await recordAuditLog(context, { modulo: 'produto-categorias', entidade: 'categoria', entidade_id: item?.id || null, acao: 'criar', descricao: 'Categoria criada', status: 'success', sucesso: true, metadata: { categoria_id: item?.id || null } }).catch(() => null); return { ok: true, item }; } catch (error) { await recordAuditLog(context, { modulo: 'produto-categorias', entidade: 'categoria', acao: 'criar', descricao: 'Falha ao criar categoria', status: 'failed', sucesso: false, erro_codigo: error?.code || 'INTERNAL_SERVER_ERROR', erro_mensagem: error?.message || 'Erro ao criar categoria' }).catch(() => null); throw error; } }
export async function updateProdutoCategoriaHandler(context = {}) { const accountId = getAccountIdFromContext(context); const body = { ...(context.body || {}) }; delete body.account_id; delete body.accountId; delete body.tenant_id; delete body.tenantId; delete body.owner_user_id; delete body.ownerUserId; try { const item = await updateProdutoCategoria(context.params?.id, body, { accountId }); await recordAuditLog(context, { modulo: 'produto-categorias', entidade: 'categoria', entidade_id: context.params?.id || null, acao: 'editar', descricao: 'Categoria editada', status: 'success', sucesso: true }).catch(() => null); return { ok: true, item }; } catch (error) { await recordAuditLog(context, { modulo: 'produto-categorias', entidade: 'categoria', entidade_id: context.params?.id || null, acao: 'editar', descricao: 'Falha ao editar categoria', status: 'failed', sucesso: false, erro_codigo: error?.code || 'INTERNAL_SERVER_ERROR', erro_mensagem: error?.message || 'Erro ao editar categoria' }).catch(() => null); throw error; } }

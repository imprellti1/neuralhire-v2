function clamp(value, min, max) { return Math.max(min, Math.min(max, value)); }
function renewalStatus(diasRestantes) { if (diasRestantes < 0) return { status: 'vencida', classificacao: 'vencida' }; if (diasRestantes <= 7) return { status: 'vence_em_breve', classificacao: 'critica' }; if (diasRestantes <= 15) return { status: 'vence_em_breve', classificacao: 'atencao' }; if (diasRestantes <= 30) return { status: 'vence_em_breve', classificacao: 'saudavel' }; return { status: 'saudavel', classificacao: 'saudavel' }; }
function expansionBand(score){ if(score<=25) return 'Baixo'; if(score<=50) return 'Moderado'; if(score<=75) return 'Alto'; return 'Excelente'; }
function churnBand(value){ if(value<=25) return 'Baixo'; if(value<=50) return 'Medio'; if(value<=75) return 'Alto'; return 'Critico'; }
export async function getCustomerRetention(accountId) { const seed=String(accountId||'').split('').reduce((a,c)=>a+c.charCodeAt(0),0)||101; const diasRestantes=(seed%60)-10; const renovacao={ diasRestantes, ...renewalStatus(diasRestantes) }; const crescimentoUso=40+(seed%61), usuariosAtivos=35+(seed%66), adocaoAlta=45+(seed%56), healthScoreAlto=30+(seed%71), churnBaixo=100-(20+(seed%61)); const expansaoScore=clamp(Math.round((crescimentoUso+usuariosAtivos+adocaoAlta+healthScoreAlto+churnBaixo)/5),0,100); const healthScore=30+(seed%71), churnRisk=20+(seed%71), automacoes=35+(seed%61), timeline=25+(seed%76), engajamento=30+(seed%71); const churnPreventivoScore=clamp(Math.round((100-healthScore+churnRisk+(100-automacoes)+(100-timeline)+(100-engajamento))/5),0,100); const churnPreventivo=churnBand(churnPreventivoScore); const acoes=[]; if(renovacao.classificacao!=='saudavel') acoes.push({tipo:'renovacao',descricao:'Agendar reuniao de sucesso.'}); if(expansaoScore>=51){ acoes.push({tipo:'expansao',descricao:'Oferecer expansao de usuarios.'}); acoes.push({tipo:'upgrade',descricao:'Oferecer plano superior.'}); } else { acoes.push({tipo:'adocao',descricao:'Reforcar treinamento.'}); } if(churnPreventivo==='Alto'||churnPreventivo==='Critico'){ acoes.push({tipo:'churn',descricao:'Revisar onboarding.'}); acoes.push({tipo:'uso',descricao:'Revisar utilizacao dos modulos.'}); } return { accountId, renovacao, expansaoScore, expansaoFaixa: expansionBand(expansaoScore), churnPreventivo, acoes }; }

import test from 'node:test'; import assert from 'node:assert/strict'; import { getExecutiveDashboard } from '../../modules/executive-dashboard/executive-dashboard.repository.js';
test('executive dashboard score consolidacao alertas tenant isolation', async()=>{ const a=await getExecutiveDashboard('acc-a'); const b=await getExecutiveDashboard('acc-b'); assert.equal(typeof a.executiveScore,'number'); assert.ok(a.executiveScore>=0 && a.executiveScore<=100); assert.ok(a.billing && a.customerSuccess && a.retention && a.implementation); assert.ok(Array.isArray(a.alertas)); assert.notEqual(a.accountId,b.accountId);});

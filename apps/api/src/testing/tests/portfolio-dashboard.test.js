import assert from 'node:assert/strict'; import { getPortfolioDashboard } from '../../modules/portfolio-dashboard/portfolio-dashboard.repository.js';
export function getPortfolioDashboardTests(){ return [{ name:'portfolio dashboard summary rankings alerts classification safety', run:async()=>{ const a=await getPortfolioDashboard(); assert.equal(a.summary.totalAccounts,5); assert.equal(a.accounts.length,5); assert.equal(a.rankings.topRevenue.length,5); assert.equal(a.alerts.length,5); assert.ok(['healthy','attention','risk','critical'].includes(a.accounts[0].status)); assert.equal(typeof a.summary.totalMrr,'number'); assert.equal(typeof a.summary.averageHealthScore,'number'); } }];}

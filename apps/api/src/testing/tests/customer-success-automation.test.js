import { createApiApp } from '../../app.js'; import { createTestRequest } from '../create-test-request.js'; import { createTestResponse } from '../create-test-response.js'; import { assertEqual } from '../assert.js';
export function getCustomerSuccessAutomationTests(){return [{name:'customer success automation cobre PB-001..PB-005 e tenant',run:async()=>{const app=createApiApp();const req=createTestRequest({method:'GET',url:'/accounts/acc-1/customer-success-automation'});const res=createTestResponse();await app(req,res);assertEqual(res.statusCode,200);const p=JSON.parse(res.body);const d=p.data||p.item||p;assertEqual(typeof d.totalAlertas,'number');assertEqual(Array.isArray(d.alertas),true);assertEqual(Array.isArray(d.playbooks),true);assertEqual(d.accountId,'acc-1');}}];}

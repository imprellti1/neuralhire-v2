import { createApiApp } from '../../app.js'; import { createTestRequest } from '../create-test-request.js'; import { createTestResponse } from '../create-test-response.js'; import { assertEqual, assertIncludes } from '../assert.js'; import { __resetMemoryInterestLeadsForTests } from '../../modules/interest-leads/interest-leads.repository.js';
export function getInterestLeadsExportTests() { return [{ name: 'GET /interest-leads/export.csv retorna csv', run: async () => { __resetMemoryInterestLeadsForTests(); const app = createApiApp(); const createReq = createTestRequest({ method: 'POST', url: '/interest-leads', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ nome: 'Ana', empresa: 'Acme', email: 'ana@acme.com' }) }); const createRes = createTestResponse(); await app(createReq, createRes); const req = createTestRequest({ method: 'GET', url: '/interest-leads/export.csv' }); const res = createTestResponse(); await app(req, res); assertEqual(res.statusCode, 200); assertIncludes(res.body, 'Nome'); } }]; }


import { createApiApp } from '../../app.js'; import { createTestRequest } from '../create-test-request.js'; import { createTestResponse } from '../create-test-response.js'; import { assertEqual } from '../assert.js'; import { __resetMemoryInterestLeadsForTests } from '../../modules/interest-leads/interest-leads.repository.js';
export function getInterestLeadsInviteTests(){return[{name:'PATCH /interest-leads/:id/invite atualiza convite',run:async()=>{__resetMemoryInterestLeadsForTests();const app=createApiApp();const c=createTestRequest({method:'POST',url:'/interest-leads',headers:{'content-type':'application/json'},body:JSON.stringify({nome:'Ana',empresa:'Acme',email:'ana@acme.com'})});const cr=createTestResponse();await app(c,cr);const id=JSON.parse(cr.body).item.id;const req=createTestRequest({method:'PATCH',url:`/interest-leads/${id}/invite`,headers:{'content-type':'application/json'},body:JSON.stringify({inviteStatus:'agendado',launchBatch:'lote-1'})});const res=createTestResponse();await app(req,res);const body=JSON.parse(res.body);assertEqual(res.statusCode,200);assertEqual(body.item.invite_status,'agendado');}}];}

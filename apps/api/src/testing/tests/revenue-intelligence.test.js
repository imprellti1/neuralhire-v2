import test from 'node:test'; import assert from 'node:assert/strict'; import { getRevenueIntelligence } from '../../modules/revenue-intelligence/revenue-intelligence.repository.js';
test('revenue intelligence mrr previsao expansao churn growth score tenant isolation', async()=>{ const a=await getRevenueIntelligence('acc-a'); const b=await getRevenueIntelligence('acc-b'); assert.ok(a.mrr>=0); assert.ok(a.receita30<=a.receita60 && a.receita60<=a.receita90); assert.ok(a.expansaoPotencial>=0); assert.ok(a.churnProjetado>=0); assert.ok(a.growthScore>=0 && a.growthScore<=100); assert.notEqual(a.accountId,b.accountId); });

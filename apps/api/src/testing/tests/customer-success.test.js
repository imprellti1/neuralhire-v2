import { createApiApp } from '../../app.js'; import { createTestRequest } from '../create-test-request.js'; import { createTestResponse } from '../create-test-response.js'; import { assertEqual } from '../assert.js';
export function getCustomerSuccessTests(){return [{name:'customer success retorna health/churn/adocao/engajamento/alertas/tenant',run:async()=>{const app=createApiApp();const req=createTestRequest({method:'GET',url:'/accounts/acc-1/customer-success'});const res=createTestResponse();await app(req,res);assertEqual(res.statusCode,200);const payload=JSON.parse(res.body);const d=payload.data||payload.item||payload;assertEqual(typeof d.healthScore,'number');assertEqual(typeof d.riscoChurn,'string');assertEqual(typeof d.adocao,'number');assertEqual(typeof d.engajamento,'number');assertEqual(Array.isArray(d.alertas),true);assertEqual(d.accountId,'acc-1');}}];}

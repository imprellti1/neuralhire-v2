import { createApiApp } from '../../app.js'; import { createTestRequest } from '../create-test-request.js'; import { createTestResponse } from '../create-test-response.js'; import { assertEqual } from '../assert.js'; import { __resetMemoryInterestLeadsForTests } from '../../modules/interest-leads/interest-leads.repository.js';
export function getInterestLeadsEventsTests() { return [{ name: 'interest-leads events list/create', run: async () => { __resetMemoryInterestLeadsForTests(); const app = createApiApp(); const createReq = createTestRequest({ method:'POST', url:'/interest-leads', headers:{'content-type':'application/json'}, body:JSON.stringify({ nome:'Ana', empresa:'Acme', email:'ana@acme.com' })}); const createRes = createTestResponse(); await app(createReq,createRes); const id = JSON.parse(createRes.body).item.id; const evReq = createTestRequest({ method:'POST', url:`/interest-leads/${id}/events`, headers:{'content-type':'application/json'}, body:JSON.stringify({ tipo:'observacao_adicionada', descricao:'primeiro contato' })}); const evRes = createTestResponse(); await app(evReq,evRes); assertEqual(evRes.statusCode,200); const listReq = createTestRequest({ method:'GET', url:`/interest-leads/${id}/events` }); const listRes = createTestResponse(); await app(listReq,listRes); assertEqual(listRes.statusCode,200); } }]; }

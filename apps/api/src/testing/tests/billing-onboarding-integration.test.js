import { createApiApp } from '../../app.js'; import { createTestRequest } from '../create-test-request.js'; import { createTestResponse } from '../create-test-response.js'; import { assertEqual } from '../assert.js';
export function getBillingOnboardingIntegrationTests(){return[{name:'billing sandbox e onboarding por account',run:async()=>{const app=createApiApp();const accountId='acc-int-1';let req=createTestRequest({method:'POST',url:`/accounts/${accountId}/subscription/prepare`,headers:{'content-type':'application/json'},body:JSON.stringify({planCode:'starter'})});let res=createTestResponse();await app(req,res);assertEqual(res.statusCode,200);assertEqual(JSON.parse(res.body).mode,'mock');req=createTestRequest({method:'GET',url:`/accounts/${accountId}/subscription`});res=createTestResponse();await app(req,res);assertEqual(res.statusCode,200);req=createTestRequest({method:'POST',url:`/accounts/${accountId}/onboarding/start`,headers:{'content-type':'application/json'},body:'{}'});res=createTestResponse();await app(req,res);assertEqual(res.statusCode,200);req=createTestRequest({method:'PATCH',url:`/accounts/${accountId}/onboarding/step`,headers:{'content-type':'application/json'},body:JSON.stringify({step:'team',data:{seats:2}})});res=createTestResponse();await app(req,res);assertEqual(res.statusCode,200);req=createTestRequest({method:'POST',url:`/accounts/${accountId}/onboarding/complete`,headers:{'content-type':'application/json'},body:JSON.stringify({checklist:true})});res=createTestResponse();await app(req,res);assertEqual(res.statusCode,200);}}];}

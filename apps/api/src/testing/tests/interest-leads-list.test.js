import { createApiApp } from '../../app.js'; import { createTestRequest } from '../create-test-request.js'; import { createTestResponse } from '../create-test-response.js'; import { assertEqual } from '../assert.js'; import { __resetMemoryInterestLeadsForTests } from '../../modules/interest-leads/interest-leads.repository.js';
export function getInterestLeadsListTests() { return [{ name: 'GET /interest-leads lista', run: async () => { __resetMemoryInterestLeadsForTests(); const app = createApiApp(); const createReq = createTestRequest({ method: 'POST', url: '/interest-leads', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ nome: 'Ana', empresa: 'Acme', email: 'ana@acme.com' }) }); const createRes = createTestResponse(); await app(createReq, createRes); const req = createTestRequest({ method: 'GET', url: '/interest-leads' }); const res = createTestResponse(); await app(req, res); const body = JSON.parse(res.body); assertEqual(res.statusCode, 200); assertEqual(Array.isArray(body.items), true); } }]; }


import { createApiApp } from '../../app.js'; import { createTestRequest } from '../create-test-request.js'; import { createTestResponse } from '../create-test-response.js'; import { assertEqual, assertIncludes } from '../assert.js';
export function getInterestLeadsValidationTests() { return [{ name: 'POST /interest-leads valida campos sensiveis', run: async () => { const app = createApiApp(); const req = createTestRequest({ method: 'POST', url: '/interest-leads', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ nome: 'Ana', empresa: 'Acme', email: 'ana@acme.com', tenant_id: 'x' }) }); const res = createTestResponse(); await app(req, res); const body = JSON.parse(res.body); assertEqual(res.statusCode, 422); assertIncludes(body.error.message, 'Campo nao permitido'); } }]; }


import { createApiApp } from '../../app.js'; import { createTestRequest } from '../create-test-request.js'; import { createTestResponse } from '../create-test-response.js'; import { assertEqual } from '../assert.js'; export function getImplementationStatusTests(){return [{name:'implementation status retorna score/ttv/risco/milestones',run:async()=>{const app=createApiApp();const req=createTestRequest({method:'GET',url:'/accounts/acc-1/implementation-status'});const res=createTestResponse();await app(req,res);assertEqual(res.statusCode,200);const payload=JSON.parse(res.body);const data=payload.data||payload.item||payload;assertEqual(typeof data.score,'number');assertEqual(Array.isArray(data.milestones),true);assertEqual(typeof data.ttv?.dias,'number');assertEqual(typeof data.risco,'string');}}];}

import { createApiApp } from '../../app.js'; import { createTestRequest } from '../create-test-request.js'; import { createTestResponse } from '../create-test-response.js'; import { assertEqual } from '../assert.js'; import { __resetMemoryInterestLeadsForTests } from '../../modules/interest-leads/interest-leads.repository.js';
export function getInterestLeadsBulkBatchTests(){return[{name:'POST /interest-leads/bulk-launch-batch aplica lote',run:async()=>{__resetMemoryInterestLeadsForTests();const app=createApiApp();const mk=async(n)=>{const c=createTestRequest({method:'POST',url:'/interest-leads',headers:{'content-type':'application/json'},body:JSON.stringify({nome:n,empresa:'Acme',email:`${n}@acme.com`})});const r=createTestResponse();await app(c,r);return JSON.parse(r.body).item.id;};const a=await mk('a'),b=await mk('b');const req=createTestRequest({method:'POST',url:'/interest-leads/bulk-launch-batch',headers:{'content-type':'application/json'},body:JSON.stringify({leadIds:[a,b],launchBatch:'lote-2'})});const res=createTestResponse();await app(req,res);assertEqual(res.statusCode,200);}}];}

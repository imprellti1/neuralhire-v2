import { getActivationStatus } from './activation.service.js';
import { mapActivation } from './activation.mapper.js';
import { createActivationState } from './activation.state.js';
export async function renderActivationPage(container,{apiClient}){ const state=createActivationState(); const acct='acc-demo'; const render=()=>{ if(state.loading){ container.innerHTML='<section><h1>Ativacao Inicial</h1><p>Carregando...</p></section>'; return; } if(state.error){ container.innerHTML='<section><h1>Ativacao Inicial</h1><p>Erro ao carregar.</p><button id="act-retry">Tentar novamente</button></section>'; container.querySelector('#act-retry')?.addEventListener('click',load); return; } const d=state.data; const rest=d.total-d.done; container.innerHTML=`<section><h1>Ativacao Inicial</h1><article style="background:#fff;border:1px solid #dbe4f2;border-radius:14px;padding:14px"><h2>Progresso da Implantacao</h2><div>${d.percentual}%</div><div style="height:8px;background:#eef3fb;border-radius:999px"><div style="width:${d.percentual}%;height:8px;background:#2563eb;border-radius:999px"></div></div><p>Voce concluiu ${d.done} de ${d.total} etapas da ativacao.</p><p>${rest>0?`Faltam ${rest} etapas.`:'Ativacao concluida.'}</p></article><div>${d.checklist.map((i)=>`<article style="margin-top:10px;background:#fff;border:1px solid #dbe4f2;border-radius:12px;padding:12px"><strong>${i.label}</strong><div>Status: ${i.status}</div><div>Data: ${i.completedAt||'-'}</div><div>Acao: ${i.action}</div></article>`).join('')}</div></section>`;}; const load=async()=>{ state.loading=true; state.error=null; render(); try{ const res=await getActivationStatus(apiClient,acct); state.data=mapActivation(res?.item||{});}catch(e){ state.error=e;} finally{ state.loading=false; render(); } }; await load(); }

import test from 'node:test'; import assert from 'node:assert/strict'; import { renderCustomerSuccessPage } from './customer-success.page.js'; import { setupFrontendDom, teardownFrontendDom, flush } from '../../testing/frontend-test-helpers.js';
test('customer-success page dom loading error retry cards score e alertas', async()=>{const dom=setupFrontendDom('#/x'); let calls=0; const apiClient={get:async()=>{calls+=1; if(calls===1) throw new Error('fail'); return {item:{healthScore:78,riscoChurn:'Baixo',engajamento:60,adocao:72,usuariosAtivos:3,usuariosTotal:5,alertas:[]}};}}; await renderCustomerSuccessPage(document.body,{apiClient}); await flush(); assert.match(document.body.textContent,/Erro ao carregar/i); document.querySelector('#cs-retry').click(); await flush(); await flush(); assert.match(document.body.textContent,/Customer Success/i); assert.match(document.body.textContent,/Health Score: 78/i); teardownFrontendDom(dom);});

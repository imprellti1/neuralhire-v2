import { createInterestLeadsDashboardState } from './interest-leads-dashboard.state.js';
export async function renderInterestLeadsDashboardPage(container,{apiClient}){container.innerHTML='<section><h1>Dashboard Lista de Interesse</h1><div id="x">Carregando...</div></section>';const st=createInterestLeadsDashboardState(apiClient);const x=container.querySelector('#x');try{const d=await st.load();x.innerHTML=`<p>Total interessados: ${d.total}</p><p>Novos hoje: ${d.novosHoje}</p><p>Ultimos 7 dias: ${d.d7}</p><p>Ultimos 30 dias: ${d.d30}</p>`;}catch{x.textContent='Erro ao carregar dashboard.';}}

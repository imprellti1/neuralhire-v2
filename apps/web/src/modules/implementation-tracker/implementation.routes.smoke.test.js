import test from 'node:test'; import assert from 'node:assert/strict'; import { bootstrapWebApp } from '../../app.js'; import { setupFrontendDom, teardownFrontendDom, flush } from '../../testing/frontend-test-helpers.js'; import { installFetchMock } from '../../testing/mocks/api-client.mock.js';
test('implementation route smoke', async()=>{const dom=setupFrontendDom('#/implementation');installFetchMock({'GET /accounts/acc-demo/implementation-status':()=>({item:{percentual:72,score:72,classificacao:'Em evolucao',ttv:{dias:5,status:'Excelente'},risco:'Baixo',milestones:[]}})});bootstrapWebApp();await flush();await flush();assert.match(document.body.textContent,/Acompanhamento de Implantacao/);teardownFrontendDom(dom);});

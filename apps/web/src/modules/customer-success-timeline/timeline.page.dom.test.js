import test from 'node:test'; import assert from 'node:assert/strict'; import { renderTimelinePage } from './timeline.page.js'; import { setupFrontendDom, teardownFrontendDom, flush } from '../../testing/frontend-test-helpers.js';
test('timeline page render filtros loading error retry empty', async()=>{const dom=setupFrontendDom('#/x'); let calls=0; const apiClient={get:async()=>{calls+=1; if(calls===1) throw new Error('fail'); return {item:{totalEventos:2,eventos:[{tipo:'onboarding_iniciado',titulo:'Onboarding iniciado',descricao:'x',data:new Date().toISOString(),origem:'onboarding'},{tipo:'automacao_disparada',titulo:'Automacao',descricao:'y',data:new Date().toISOString(),origem:'customer-success-automation'}]}};}}; await renderTimelinePage(document.body,{apiClient}); await flush(); assert.match(document.body.textContent,/Erro ao carregar/i); document.querySelector('#tl-retry').click(); await flush(); await flush(); assert.match(document.body.textContent,/Timeline CS/i); document.querySelector('[data-f="onboarding"]').click(); await flush(); assert.match(document.body.textContent,/Onboarding iniciado/i); teardownFrontendDom(dom);});

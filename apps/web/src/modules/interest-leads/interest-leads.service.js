export function createInterestLeadsService(apiClient) { return { async list(params = {}) { return apiClient.get('/interest-leads', params); }, async updateStatus(id, status) { return apiClient.patch(`/interest-leads/${id}/status`, { status }); }, exportCsvUrl(params = {}) { const q = new URLSearchParams(params); return `/interest-leads/export.csv${q.toString() ? `?${q}` : ''}`; } }; }

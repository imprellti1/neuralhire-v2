export function createInterestLeadsLaunchService(apiClient){return{dashboard:()=>apiClient.get('/interest-leads/launch-dashboard'),list:(p={})=>apiClient.get('/interest-leads',p),invite:(id,p)=>apiClient.patch(`/interest-leads/${id}/invite`,p),bulk:(leadIds,launchBatch)=>apiClient.post('/interest-leads/bulk-launch-batch',{leadIds,launchBatch}),listTemplates:()=>apiClient.get('/launch/templates'),preview:(payload)=>apiClient.post('/launch/preview',payload),queue:(payload)=>apiClient.post('/launch/queue',payload)}}

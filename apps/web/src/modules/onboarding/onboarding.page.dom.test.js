import test from 'node:test'; import assert from 'node:assert/strict'; import { renderOnboardingPage } from './onboarding.page.js'; import { setupFrontendDom, teardownFrontendDom, flush } from '../../testing/frontend-test-helpers.js';
test('onboarding page dom', async()=>{ const dom=setupFrontendDom('#/onboarding'); const apiClient={ get:async()=>({ok:true,item:{current_step:'company',completed_steps:['welcome']}}), patch:async()=>({ok:true}), post:async()=>({ok:true})}; await renderOnboardingPage(document.body,{apiClient}); await flush(); assert.match(document.body.textContent,/Progresso/); document.querySelector('#onb-next').click(); await flush(); document.querySelector('#onb-complete').click(); await flush(); assert.match(document.body.textContent,/Conta pronta/); teardownFrontendDom(dom);});

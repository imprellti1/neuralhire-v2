import test from 'node:test'; import assert from 'node:assert/strict'; import { bootstrapWebApp } from '../../app.js'; import { setupFrontendDom, teardownFrontendDom, flush } from '../../testing/frontend-test-helpers.js'; import { installFetchMock } from '../../testing/mocks/api-client.mock.js';
test('onboarding route smoke', async()=>{ const dom=setupFrontendDom('#/onboarding'); installFetchMock({'GET /accounts/acc-demo/onboarding':()=>({ok:true,item:{current_step:'welcome',completed_steps:[]}})}); bootstrapWebApp(); await flush(); await flush(); assert.match(document.body.textContent,/Onboarding/); teardownFrontendDom(dom);});

import test from 'node:test'; import assert from 'node:assert/strict'; import { renderAutomationPage } from './automation.page.js'; import { setupFrontendDom, teardownFrontendDom, flush } from '../../testing/frontend-test-helpers.js';
test('automation page dom render cards alertas loading error retry', async()=>{const dom=setupFrontendDom('#/x'); let calls=0; const apiClient={get:async()=>{calls+=1; if(calls===1) throw new Error('fail'); return {item:{totalAlertas:2,criticos:0,altos:1,medios:1,baixos:0,playbooks:['PB-001'],alertas:[{playbook:'PB-001',titulo:'Conta em risco',severidade:'alta',descricao:'x'}]}};}}; await renderAutomationPage(document.body,{apiClient}); await flush(); assert.match(document.body.textContent,/Erro ao carregar/i); document.querySelector('#auto-retry').click(); await flush(); await flush(); assert.match(document.body.textContent,/Automações CS/i); assert.match(document.body.textContent,/Total de alertas: 2/i); teardownFrontendDom(dom);});

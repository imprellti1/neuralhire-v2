import { getCustomerSuccessAutomation } from './automation.service.js';
import { mapCustomerSuccessAutomation } from './automation.mapper.js';
import { createAutomationState } from './automation.state.js';
export async function renderAutomationPage(container,{apiClient}){ const state=createAutomationState(); const acct='acc-demo'; const render=()=>{ if(state.loading){container.innerHTML='<section><h1>Automações CS</h1><p>Carregando...</p></section>'; return;} if(state.error){container.innerHTML='<section><h1>Automações CS</h1><p>Erro ao carregar</p><button id="auto-retry">Tentar novamente</button></section>'; container.querySelector('#auto-retry')?.addEventListener('click',load); return;} const d=state.data||{}; const rows=(d.alertas||[]).map((a)=>`<tr><td>${a.playbook}</td><td>${a.titulo}</td><td>${a.severidade}</td><td>${a.descricao}</td></tr>`).join(''); container.innerHTML=`<section><h1>Automações CS</h1><div>Total de alertas: ${d.totalAlertas||0}</div><div>Criticos: ${d.criticos||0}</div><div>Altos: ${d.altos||0}</div><div>Medios: ${d.medios||0}</div><div>Baixos: ${d.baixos||0}</div><div>Playbooks acionados: ${(d.playbooks||[]).join(', ')||'-'}</div><table><thead><tr><th>PB</th><th>Titulo</th><th>Severidade</th><th>Descricao</th></tr></thead><tbody>${rows}</tbody></table></section>`;}; const load=async()=>{state.loading=true;state.error=null;render(); try{state.data=mapCustomerSuccessAutomation(await getCustomerSuccessAutomation(apiClient,acct));}catch(e){state.error=e;} finally{state.loading=false;render();}}; await load(); }

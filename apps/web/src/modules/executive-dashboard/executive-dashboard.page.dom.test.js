import test from 'node:test'; import assert from 'node:assert/strict'; import { renderExecutiveDashboardPage } from './executive-dashboard.page.js'; import { setupFrontendDom, teardownFrontendDom, flush } from '../../testing/frontend-test-helpers.js';
test('executive-dashboard page dom cards alertas loading error retry', async()=>{const dom=setupFrontendDom('#/x'); let calls=0; const apiClient={get:async()=>{calls+=1; if(calls===1) throw new Error('fail'); return {item:{executiveScore:82,customerSuccess:{healthScoreMedio:78},retention:{churnPreventivo:'Baixo',expansaoPotencial:1,renovacoesProximas:1},alertas:[{tipo:'critico',descricao:'Conta com churn critico.'}]}};}}; await renderExecutiveDashboardPage(document.body,{apiClient}); await flush(); assert.match(document.body.textContent,/Erro ao carregar/i); document.querySelector('#ed-retry').click(); await flush(); await flush(); assert.match(document.body.textContent,/Executive Dashboard/i); assert.match(document.body.textContent,/Executive Score: 82/i); teardownFrontendDom(dom);});

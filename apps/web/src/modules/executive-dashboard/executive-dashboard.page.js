import { getExecutiveDashboard } from './executive-dashboard.service.js'; import { mapExecutiveDashboardResponse } from './executive-dashboard.mapper.js'; import { createExecutiveDashboardState } from './executive-dashboard.state.js';
export async function renderExecutiveDashboardPage(container,{apiClient}){ const state=createExecutiveDashboardState(); const acct='acc-demo'; const render=()=>{ if(state.loading){container.innerHTML='<section><h1>Executive Dashboard</h1><p>Carregando...</p></section>'; return;} if(state.error){container.innerHTML='<section><h1>Executive Dashboard</h1><p>Erro ao carregar</p><button id="ed-retry">Tentar novamente</button></section>'; container.querySelector('#ed-retry')?.addEventListener('click',load); return;} const d=state.data||{}; const a=(d.alertas||[]).map((x)=>`<li>${x.tipo}: ${x.descricao}</li>`).join(''); container.innerHTML=`<section><h1>Executive Dashboard</h1><div>Executive Score: ${d.executiveScore??'-'}</div><div>Health Medio: ${d.customerSuccess?.healthScoreMedio??'-'}</div><div>Churn: ${d.retention?.churnPreventivo||'-'}</div><div>Expansao: ${d.retention?.expansaoPotencial??0}</div><div>Renovacoes: ${d.retention?.renovacoesProximas??0}</div><div>Alertas: ${(d.alertas||[]).length}</div><h2>Billing</h2><h2>CS</h2><h2>Implantacao</h2><h2>Retencao</h2><ul>${a}</ul></section>`; }; const load=async()=>{state.loading=true;state.error=null;render(); try{state.data=mapExecutiveDashboardResponse(await getExecutiveDashboard(apiClient,acct));}catch(e){state.error=e;} finally{state.loading=false;render();}}; await load(); }

import test from 'node:test'; import assert from 'node:assert/strict'; import { renderRevenuePage } from './revenue.page.js'; import { setupFrontendDom, teardownFrontendDom, flush } from '../../testing/frontend-test-helpers.js';
test('revenue page render cards loading error retry', async()=>{ const dom=setupFrontendDom('#/x'); let calls=0; const apiClient={get:async()=>{calls+=1; if(calls===1) throw new Error('fail'); return {item:{mrr:12500,receita30:15000,receitaEmRisco:3200,expansaoPotencial:4800,churnProjetado:6.5,growthScore:82}};}}; await renderRevenuePage(document.body,{apiClient}); await flush(); assert.match(document.body.textContent,/Erro ao carregar/i); document.querySelector('#rev-retry').click(); await flush(); await flush(); assert.match(document.body.textContent,/Revenue Intelligence/i); assert.match(document.body.textContent,/MRR: 12500/i); teardownFrontendDom(dom); });
